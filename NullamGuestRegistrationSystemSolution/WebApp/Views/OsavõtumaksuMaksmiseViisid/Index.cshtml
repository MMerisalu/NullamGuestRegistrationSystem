@model IEnumerable<App.Domain.OsavõtumaksuMaksmiseViis>

@{
    ViewData["Title"] = "Makseviisid";
}

<h1>Makseviisid</h1>

<p>
    <a style="text-decoration:none" asp-action="Create">MAKSEVIISI LISAMINE</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.OsavõtumaksuMaksmiseViisiNimetus)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.OsavõtumaksuMaksmiseViisiNimetus)
            </td>
            <td>
                <a style="text-decoration:none" asp-action="Edit" asp-route-id="@item.Id">MUUDA</a> |
                <a style="text-decoration:none" asp-action="Delete" asp-route-id="@item.Id">KUSTUTA</a>
            </td>
        </tr>
}
    </tbody>
</table>
